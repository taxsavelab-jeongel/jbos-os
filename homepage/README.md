# (주)정엘기업연구소 홈페이지

> **법인 구조 메모**
> `(주)정엘기업연구소`가 법인이고, `정엘가업승계연구소`는 이 법인이 운영하는 **가업승계 전문 브랜드**입니다.
> 따라서 사업자등록번호(208-88-01033)·통신판매업신고번호·주소·연락처·신뢰 지표는 **양쪽이 동일**하며,
> 이 사이트에 그대로 사용합니다. 푸터와 JSON-LD(`legalName` / `brand`)에 관계를 명시해 두었습니다.

> ## 🚧 검토 중 — 아직 배포하지 마십시오
>
> 이 사이트는 **아직 어디에도 배포되지 않았습니다.** GitHub 브랜치 안의 파일일 뿐입니다.
> 충분히 검토·수정해 완성된 뒤에 대표님 승인을 받아 배포합니다.
>
> | 도메인 | 용도 | 상태 |
> |---|---|---|
> | `jeongellab.com` | **정엘기업연구소 — 이 사이트** | 배포 전 |
> | `lp.jeongellab.com` | 상담 신청 랜딩페이지 | 운영 중 (건드리지 않음) |
> | `jeongel.co.kr` | 정엘가업승계 홈페이지 (별개 사이트) | 운영 중 (건드리지 않음) |
>
> **배포 전 확인이 필요한 항목**은 이 문서 맨 아래 「배포 전 확인 목록」에 모아 두었습니다.

가업승계·중소기업 M&A 전문 세무법인 홈페이지를 벤치마킹해, **정엘기업연구소 브랜드 아이덴티티**로
새로 설계한 정적 웹사이트입니다. 의존성 없는 HTML/CSS/JS 3파일 구성이며 그대로 업로드하면 동작합니다.

```
homepage/
├── index.html              # 전체 페이지
└── assets/
    ├── styles.css          # 브랜드 디자인 시스템
    ├── calculators.js      # 진단 도구 계산 엔진 (세법 로직)
    ├── site.js             # 내비게이션 · 계산기 UI · 폼
    └── mark.svg            # 파비콘 / 심볼
```

## 브랜드 적용 근거

Google Drive `01.정엘그룹 운영팀 / 00_그룹공통 / 02_브랜드` 자료를 그대로 반영했습니다.

| 요소 | 적용 내용 | 출처 |
|---|---|---|
| 컬러 | 아래 표 참조 | **로고 원본 + 공식 컬러칩** |
| 브랜드 아이덴티티 | “가업승계 그 이상, 100년 기업 아키텍트” | 브랜드 하우스 final |
| 브랜드 약속 | Sustainable Prosperity · 지속가능한 번영 | 브랜드 하우스 final |
| 3L 철학 | Loyal(Trustworthy) / Leap(Understated) / Lead(Best in Class) | 브랜드지침 v1.0 §2 |
| 대표 슬로건 | “승계가 필요한 그 순간을 넘어, 진심을 담은 실력으로 보답하겠습니다.” | 브랜드지침 v1.0 §1 (변경 금지) |
| Perspective Change | 착실한 조력자 → 믿음직한 사업 동반자 (5개 축) | 브랜드 하우스 final |
| 상담 프로세스 | 3단계 (상담·실행·기업돌봄) 및 상담료 표기 | 브랜드지침 v1.0 §5 |
| 신뢰 지표 | 회원사 38,029+ / 컨설팅 211,585+ / 컨설턴트 1,028명 | 브랜드지침 v1.0 §6 (연구소 수치만 사용) |
| 연락처·사업자 정보 | 서초구 반포대로 79 5층 / 02-523-3757 / 상담 010-5500-9632 | 브랜드지침 v1.0 §7 |

브랜드지침 §8 금지사항(공포마케팅, “무조건·100%·세금 없이” 류의 과장 표현)은 전체 카피에서 배제했습니다.

### 컬러 시스템 (공식 컬러칩 기준)

| Pantone | HEX | 토큰 | 용도 |
|---|---|---|---|
| **2449 C** | `#381513` | `--brand` | 주색 — 로고 워드마크. 어두운 섹션 배경, 제목, 유틸리티 버튼 |
| **3517 C** | `#C0111F` | `--accent` | 포인트 — 로고 `L` 마크. 전환 CTA, 키커, 경고 수치, 강조 라인 |
| 7541 C | `#E2ECF2` | `--mist` `--line` | 구분선, 태그·근거박스 배경 |
| 428 C | `#CAD0D7` | `--steel` `--line-deep` | 입력 필드 테두리 |
| 5503 C | `#98BEC4` | `--sage` `--hi` | **어두운 배경 위 강조색** |

파생 토큰: `--ink #1C0A09`(푸터·호버) · `--brand-2 #4A211C`(그라디언트) ·
`--accent-deep #930D18`(버튼 호버) · `--accent-soft #F9EAEC`(강조 박스 배경) ·
`--sage-deep #6B939A`(밝은 배경에서 대비 확보용).

**규칙 두 가지**

1. **강조색은 배경에 따라 다릅니다.** 어두운 배경에서는 레드가 묻히므로 세이지(`.hi`)를,
   밝은 배경에서는 레드(`.accent`)를 씁니다. 클래스가 나뉘어 있는 이유입니다.
2. **레드는 아껴 씁니다.** 전환 CTA, 경고 수치(`.row.warn`), 강조 라인에만 사용합니다.
   계산 결과에서 유리한 쪽 금액은 레드가 아니라 브랜드 브라운으로 강조합니다
   (레드가 “위험”으로 읽히지 않도록).

> 초기 버전은 `브랜드전략_7단계.docx`의 “딥그린 + 골드” 기술을 따랐으나,
> 로고 원본과 공식 컬러칩 확인 결과 실제 브랜드는 **딥브라운 + 레드**였습니다.
> 해당 전략 문서의 컬러 기술은 실제 아이덴티티와 다르므로 참조하지 마십시오.
> J-BOS 업무 OS(`../styles.css`)의 `--brand: #381513` · `--line: #e2ecf2` ·
> `--line-strong: #cad0d7` 가 이 팔레트와 일치합니다.

## 전문 진단 도구 7종

계산기 명칭은 “○○ 계산기” 대신 **전문성과 직관성을 함께 갖춘 명칭**으로 교체했습니다.

| # | 명칭 | 하는 일 | 근거 법령 |
|---|---|---|---|
| 1 | **승계세액 비교 시뮬레이터** | 가업승계 증여특례 vs 일반 증여세율 세액 비교 | 조특법 §30의6, 상증법 §26 |
| 2 | **상속세 부담 진단기** | 일괄·배우자·금융재산·가업상속공제 반영 납부세액 | 상증법 §18의2·19·21·22 |
| 3 | **기업가치 평가 시뮬레이터** | 비상장주식 1주당 평가액 및 보유지분 가치 | 상증법 §63, 시행령 §54 |
| 4 | **가지급금 손실 진단기** | 연간 반복 세부담 + 특수관계 소멸 시 일시 과세 | 법인세법 §28·52, 시행규칙 §43 |
| 5 | **임원 퇴직금 한도 설계기** | 퇴직소득 한도 판정 및 세후 실수령액 | 소득세법 §22④2, §48 |
| 6 | **이익잉여금 인출전략 비교기** | 급여·배당·퇴직금 3경로 실수령 비교 | 소득세법 §55·129, 법인세법 §55 |
| 7 | **사전증여 분산 설계기** | 10년 합산 주기를 활용한 분산 증여 효과 | 상증법 §53·57 |

### 계산 엔진 설계 원칙

- 세율표는 `calculators.js` 상단 `TABLE`에 **누진공제 방식**으로 분리해 두었습니다.
  세법이 개정되면 이 표만 수정하면 전 계산기에 반영됩니다.
- 지방소득세 10%는 `LOCAL` 계수, 신고세액공제 3%는 `FILING_CREDIT`,
  당좌대출이자율 4.6%는 `LOAN_RATE`, 순손익가치 환원율 10%는 `EARN_RATE` 상수로 관리합니다.
- 퇴직소득세는 근속연수공제 → 환산급여 → 환산급여공제 → 12분의 1 방식까지 실제 산식을 구현했습니다.
- 6번 비교기는 **회사가 부담하는 세전 재원을 세 경로 모두 동일하게 맞춰** 비교합니다.
  (배당은 손금이 아니므로 법인세를 먼저 차감한 금액만 지급 가능하도록 처리)
- 모든 계산기는 입력 누락·0·음수에 대한 가드를 두어 `NaN`이 화면에 노출되지 않습니다.

### 검증

`node` 환경에서 세율표 검산 및 7개 계산기 전수 엣지케이스 테스트를 통과했습니다.
(증여 과표 10억 → 2.4억, 30억 → 10.4억, 50억 → 20.4억 등 법정 세액과 일치)

## SEO — 검색 노출

목표는 **네이버 · 구글 · AI 검색(ChatGPT·Perplexity·구글 AI 개요)** 세 곳 모두입니다.

### 적용한 것

| 항목 | 내용 |
|---|---|
| 메타 | title(키워드 선행형)·description·canonical·robots(max-snippet 무제한)·author |
| 소셜 | og:* 전체 + twitter card + **1200×630 OG 이미지**(`assets/og-image.png`) |
| 구조화 데이터 | JSON-LD `@graph` — ProfessionalService/Organization, WebSite, WebPage, ItemList(도구 7종), FAQPage(5문항) |
| 크롤링 | `robots.txt` · `sitemap.xml` · `llms.txt` |
| 국내 검색 | 네이버 `Yeti`, 다음 `Daumoa` 명시적 허용 |
| AI 검색 | GPTBot·OAI-SearchBot·ClaudeBot·PerplexityBot·Google-Extended·Applebot-Extended **명시적 허용** |
| 시맨틱 | h1 1개 / h2 7개 / h3 14개 계층, 푸터 `<address>`, `lang="ko"` |

### 핵심 수정: 계산기 콘텐츠를 크롤러가 볼 수 있게 만든 것

계산기 UI는 `calculators.js`가 런타임에 생성하므로 **HTML 소스에는 존재하지 않았습니다.**
`승계세액 비교 시뮬레이터`, `가지급금 손실 진단기` 등 도구 7종 중 4종의 이름과
근거 법령이 통째로 검색엔진에 안 보이는 상태였습니다.

→ `.tools-index` 블록을 정적 HTML로 추가했습니다. 도구 7종의 이름·설명·근거 법령이
본문에 그대로 들어가며, 사용자에게도 색인 역할을 합니다.
정적 본문 글자수 **4,120자 → 5,030자**, 도구 키워드 **3/9 → 9/9** 노출.

### 배포 후 반드시 할 일

1. **네이버 서치어드바이저** (searchadvisor.naver.com) — 사이트 등록 → 소유확인 메타태그를
   `index.html` 14행 주석에 넣고 주석 해제 → 사이트맵 제출 → 웹페이지 수집 요청
2. **구글 서치콘솔** (search.google.com/search-console) — 동일하게 소유확인 후 사이트맵 제출
3. **다음 검색등록** (register.search.daum.net)
4. 도메인이 `jeongellab.com`이 아니라면 아래 한 줄로 일괄 교체:
   ```bash
   grep -rl 'jeongellab.com' homepage/ | xargs sed -i 's|https://jeongellab.com|https://실제도메인|g'
   ```

### 도구 페이지 7개 (검색어별 독립 URL)

1페이지 사이트는 색인될 URL이 하나뿐이라 롱테일 검색어를 잡을 수 없습니다.
도구마다 **독립 URL + 고유 title + 본문 콘텐츠**를 가진 페이지를 만들었습니다.

| URL | 노리는 검색어 | 본문 |
|---|---|---|
| `/tools/succession-gift-tax/` | 가업승계 증여세 계산기 | 특례 개요·한도·사업무관자산·사후관리 |
| `/tools/inheritance-tax/` | 상속세 계산기 | 계산 순서·세율표·주요 공제·사전증여 합산 |
| `/tools/stock-valuation/` | 비상장주식 가치평가 계산기 | 보충적 평가·순손익가치·80% 하한·최대주주 할증 |
| `/tools/directors-loan/` | 가지급금 계산기 | 인정이자·상여처분·특수관계 소멸·승계 영향 |
| `/tools/executive-severance/` | 임원 퇴직금 한도 계산기 | 두 개의 한도·구간별 배수·환산급여 방식 |
| `/tools/retained-earnings/` | 이익잉여금 인출 계산기 | 세 경로 비교 기준·급여/배당/퇴직금 각각의 성격 |
| `/tools/gift-planning/` | 사전증여 계산기 | 10년 주기·분산 효과·상속 합산·세대생략 할증 |

각 페이지 구성: 브레드크럼 → h1 → **해당 계산기 하나** → 개념 설명(h2 4~5개) →
FAQ 3문항 → 다른 도구 6개 링크 → 상담 CTA. 페이지당 본문 3,100~3,500자.

구조화 데이터도 페이지마다 붙습니다 — `WebApplication`(무료 도구), `BreadcrumbList`, `FAQPage`.

**생성 방식**: `tools-content.mjs`(콘텐츠) + `build-tools.mjs`(템플릿)로 생성합니다.
헤더·푸터·CTA가 바뀌면 `build-tools.mjs`만 고치고 다시 돌리면 7개에 한 번에 반영됩니다.

```bash
node build-tools.mjs   # tools/*/index.html 7개 + sitemap.xml(8 URL) 재생성
```

정적 호스팅이라 빌드 단계 없이 올라가야 하므로 **생성물도 함께 커밋**합니다.
`tools/` 아래 파일을 직접 수정하면 다음 생성 때 덮어쓰이니, 반드시 소스 두 파일을 고치십시오.

### 다음 단계

1. **인사이트 아티클** — 세법 개정, 판례, 사례를 정기 발행. 네이버는 갱신 빈도를 크게 봅니다.
2. **유튜브 채널 연결** — JSON-LD `sameAs`에 채널 URL 추가 (현재 미보유로 생략).
3. **웹폰트 자체 호스팅** — Google Fonts 외부 요청이 LCP를 늦춥니다. Core Web Vitals 개선용.

## 배포

정적 파일이므로 `homepage/` 폴더 내용을 그대로 올리면 됩니다.
회사 표준 배포 경로(Cloudflare Workers, `jeongellab.com` 서브도메인)를 쓰는 경우
`jeongel-web-deploy` 스킬의 절차를 따르십시오. **「배포」 버튼까지 눌러야 반영됩니다.**

로컬 확인:

```bash
python3 -m http.server 8899 --directory homepage
# http://127.0.0.1:8899
```

## 상담 신청 동선

정엘의 **모든 상담 신청 링크는 `https://lp.jeongellab.com` 하나로 통일**합니다.
홈페이지는 자체 신청 폼을 두지 않고, 모든 CTA를 이 랜딩페이지로 보냅니다.
신청 접수·저장·알림은 전부 그 페이지가 담당하므로 유입 경로가 갈라지지 않습니다.

CTA 위치 4곳:

| 위치 | 문구 |
|---|---|
| 헤더 내비게이션 | 상담 신청 |
| 히어로 | 승계 설계 상담 신청 |
| 계산기 결과 하단 | 상담 신청하기 |
| CONTACT 섹션 | 상담 신청하기 |

주소는 `assets/site.js` 상단 `LP_URL` 상수와 `index.html` 링크에 있습니다.
도메인이 바뀌면 이 두 곳만 고치면 됩니다.

전화(`010-5500-9632`)는 신청 링크가 아니라 즉시 연결 수단이므로 `tel:` 링크로 유지합니다.

> `integration/` 폴더에는 홈페이지에 자체 폼을 붙일 때 쓰는 구글폼·Apps Script 연동 코드가
> 들어 있습니다. **현재 신청 동선이 랜딩페이지로 통일되어 실행하지 않습니다.**
> 자세한 내용은 [`integration/README.md`](integration/README.md) 상단 안내를 확인하십시오.

## 참고

- **웹폰트**: Noto Serif KR / Noto Sans KR을 Google Fonts에서 불러옵니다.
  폰트 로드가 실패해도 시스템 명조·고딕으로 자연스럽게 대체됩니다.

---

## 배포 전 확인 목록

배포 전에 대표님 확인이 필요한 항목입니다. **확인 전에는 올리지 마십시오.**

### 1. 사업자 정보 (법적 표시 사항 — 가장 중요)

푸터와 JSON-LD에 들어간 아래 정보는 **브랜드지침 v1.0 §7의 「정엘가업승계연구소」 기준**입니다.
(주)정엘기업연구소의 정보와 다를 수 있습니다.

| 항목 | 현재 값 | 확인 |
|---|---|---|
| 사업자등록번호 | 208-88-01033 | ❓ 정엘기업연구소 번호로 교체 필요 여부 |
| 통신판매업신고 | 제2025-서울서초-0459호 | ❓ 동일 |
| 주소 | 서울특별시 서초구 반포대로 79, 5층 | ❓ 동일 사무실인지 |
| 대표번호 | 02-523-3757 | ❓ |
| 이메일 | taxsavelab@gmail.com | ❓ |

전자상거래법상 사업자 정보는 정확해야 하므로, **틀린 번호가 올라가면 안 됩니다.**

### 2. 신뢰 지표 숫자

히어로 하단의 회원사 38,029+ / 컨설팅 211,585+ / 컨설턴트 1,028명 / 15년은
브랜드지침 §6의 **「연구소(jeongel.co.kr)」 수치**입니다. §6은 *"부문 간 혼용하지 않는다"* 고
명시하고 있으므로, 정엘기업연구소 기준 수치가 따로 있다면 교체해야 합니다.

### 3. 콘텐츠 방향

현재 내용은 전부 가업승계·절세 중심입니다. 정엘기업연구소의 사업 범위가
더 넓다면(컨설팅·강의·자문 등) 솔루션 섹션을 조정해야 합니다.

### 4. 도메인 최종 확인

`jeongellab.com` 루트에 이 사이트를 올리는 것이 맞는지 확인이 필요합니다.
`lp.jeongellab.com`(상담 신청)과 `golden.jeongellab.com`(골든타임진단)이
이미 서브도메인으로 운영 중이므로, 루트 배포가 기존 서브도메인에 영향을 주지 않는지
Cloudflare 설정을 함께 점검하십시오.
